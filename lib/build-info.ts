/**
 * مُهر بیلد — پایان دادن به کلاس باگ «بیلد کهنه» (خواسته‌ی کاربر):
 * کاربر یک بار خودش فهمید کرش next/image از بیلدِ عقب‌مانده بود؛ همان
 * بیماری می‌تواند بی‌صدا «عکس نمی‌آید / فیچر نیست» تولید کند. از این به بعد
 * هش گیتِ بیلد، کنار لوگو دیده می‌شود تا در یک نگاه مشخص باشد کدِ اجرا‌شده
 * با ریپو هم‌خوان است یا نه.
 *
 * هش در زمان build/start توسط next.config.ts از گیت خوانده و به
 * NEXT_PUBLIC_BUILD_ID تزریق می‌شود؛ بیرون از گیت (مثلا باندل نهایی بدون
 * .git) «؟» می‌بینیم — خودش هم سیگنال است.
 */
export const BUILD_ID: string = process.env.NEXT_PUBLIC_BUILD_ID?.trim() || "؟";
